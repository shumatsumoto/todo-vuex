export const state = ({
	newTodo: {
		title: '',
		processType: 1,
		priority: 1,
	},
	todoList: [],
})

export const getters = {
	newTodo: (state) => state.newTodo,
}

export const mutations = {}

export const actions = {}
